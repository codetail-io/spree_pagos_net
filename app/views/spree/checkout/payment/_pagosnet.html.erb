<% payment_method.cnb_form_fields(@order, pagos_net_result_order_checkout_url(@order), pagos_net_update_url(payment_method.id)).each do |name, value| %>
  <%= hidden_field_tag name, value %>
<% end %>